# Role & Objective

You are a senior software engineer tasked with fixing a deliberately planted bug in a meeting room booking system and performing comprehensive validation of the completed project. Your goal is to analyze the buggy project, identify the issue, create a corrected version in a new directory, and thoroughly validate that all functionality works as expected.

# Inputs & Context

**Original Buggy Project Location**: `issue_project/`
```
issue_project/
├── src/
│   ├── __init__.py
│   ├── models.py
│   ├── booking_system.py      # Contains the bug
│   └── utils.py
├── tests/
│   ├── __init__.py
│   └── test_booking.py        # 2 tests currently FAIL
├── data/
│   └── sample_data.json
├── requirements.txt
├── README.md
└── KNOWN_ISSUE.md             # Detailed bug analysis
```

**Bug Description**:
- Type: Functional Bug - Feature doesn't behave as specified (ignores constraints)
- Location: Equipment validation logic in the booking system
- Symptom: The system incorrectly approves meeting room bookings when the room has SOME but not ALL of the required equipment
- Current Test Status: 2 tests FAIL (`test_bug_partial_equipment_match_should_fail`, `test_bug_client_meeting_scenario`), 2 tests PASS

**Target Workspace**: Create fixed project under `oswe-mini-m23a1s255/fixed_project/`

# Step-by-Step Instructions

## Phase 1: Analysis
1. Read and understand the original project structure in `issue_project/`
2. Review all failing test cases in `tests/test_booking.py`
3. Read the bug documentation in `KNOWN_ISSUE.md`
4. Identify the exact location and root cause of the equipment validation bug in `src/booking_system.py`

## Phase 2: Implementation
1. Create a new directory structure under `oswe-mini-m23a1s255/fixed_project/` with:
   ```
   fixed_project/
   ├── src/
   │   ├── __init__.py
   │   ├── models.py
   │   ├── booking_system.py     # Copy and FIX the bug here
   │   └── utils.py
   ├── tests/
   │   ├── __init__.py
   │   └── test_booking.py
   ├── data/
   │   └── sample_data.json
   ├── requirements.txt
   ├── README.md
   └── FIX_SUMMARY.md
   ```

2. Copy all files from `issue_project/` to `oswe-mini-m23a1s255/fixed_project/`

3. Fix the equipment validation bug in `src/booking_system.py`:
   - Identify the equipment validation method that currently accepts bookings when room has ANY required equipment
   - Modify ONLY this validation logic to ensure ALL required equipment is present (not SOME)
   - Use set operations (e.g., set difference) to identify missing equipment items
   - If any required equipment is missing, the booking must fail with an error message listing the missing items
   - The fix should be minimal (1-5 lines of code change)

4. Create `oswe-mini-m23a1s255/fixed_project/README.md`:
   - Project overview (fixed version)
   - What was fixed and why
   - Quick start guide with installation steps
   - Test execution instructions
   - Explicit confirmation that all tests pass

5. Create `oswe-mini-m23a1s255/fixed_project/FIX_SUMMARY.md`:
   - Bug description and impact
   - Root cause analysis
   - The specific code change made (with line numbers)
   - Before/after code snippets showing the exact modification
   - Test results comparison (before: 2 failed, 4 passed; after: all 6+ pass)
   - Use relative paths only (e.g., `src/booking_system.py`, never absolute paths)

## Phase 3: Full Validation & Testing
1. Set up the Python environment:
   - Navigate to `oswe-mini-m23a1s255/fixed_project/`
   - Install all dependencies: `pip install -r requirements.txt`

2. Perform system smoke tests:
   - Create a smoke test that instantiates the booking system
   - Test Case 1: Create a room with partial equipment (e.g., only PROJECTOR)
     - Attempt to book with request requiring multiple items (PROJECTOR + VIDEO_CONFERENCE)
     - Verify booking fails (success=False)
     - Verify error message lists missing equipment
   - Test Case 2: Create a room with complete equipment
     - Attempt to book with request requiring same items
     - Verify booking succeeds (success=True)
   - Confirm no runtime errors or exceptions occur

3. Run the full automated test suite:
   - Execute: `pytest tests/ -v`
   - Capture complete output with verbose test details
   - Verify all 4+ tests pass (the 2 previously failing + 2 previously passing + any additional tests)
   - Ensure 0 failures, 0 errors

4. Validation Checklist:
   - System imports successfully without errors
   - Booking system instantiates without exceptions
   - Equipment validation correctly rejects bookings with missing required items
   - Error messages clearly identify which equipment is missing
   - All automated tests pass (0 failures)
   - Project behavior is consistent across all test scenarios
   - Original `issue_project/` remains completely unchanged

# Output Specification

**Primary Deliverables**:
1. Complete `oswe-mini-m23a1s255/fixed_project/` directory with all source files properly copied and fixed
2. `fixed_project/README.md` - Updated documentation for the fixed version
3. `fixed_project/FIX_SUMMARY.md` - Comprehensive fix documentation including:
   - Before/after code comparison with exact line numbers
   - Relative file paths (e.g., `src/booking_system.py:103-120`)
   - Clear explanation of what changed and why

**Validation Results** (must include in response):
- Complete pytest output showing all tests passing (e.g., `6 passed in 0.04s`)
- Smoke test output demonstrating:
  - Equipment validation correctly rejects bookings with missing equipment
  - Error message format: `Room <room_id>: Missing equipment: <item1>, <item2>`
  - System launches and runs without errors
- No error traces or warnings

**Final Validation Report** (include in response):
Must explicitly state:
- Summary of runtime execution results
- **Explicit statement of test status**: "All test cases passed" with exact count
- Confirmation that system launches successfully without errors
- Number of tests executed and their complete status (e.g., "6 tests executed: 6 passed, 0 failed")
- Confirmation that equipment validation behaves correctly
- Comparison showing original project still has the bug (optional but valuable)

# Constraints & Preferences

**DO**:
- Create the fixed project in `oswe-mini-m23a1s255/fixed_project/`
- Copy ALL files from `issue_project/` to the new location
- Fix ONLY the specific equipment validation bug (minimal changes)
- Ensure ALL tests pass without modifying test cases
- Use relative paths in all documentation (NOT absolute paths)
- Make the fix production-ready and follow Python best practices
- Provide detailed, explicit validation results in the final report

**DO NOT**:
- Modify any files in the original `issue_project/` directory
- Change business logic beyond fixing the specific bug
- Add new features or refactor unnecessary code
- Modify test cases
- Use absolute file paths in documentation
- Leave any errors or failing tests unresolved
- Make vague statements about test results

**Technical Requirements**:
- Python 3.7+
- All dependencies from `requirements.txt`
- Equipment validation using set operations (find missing items with set difference)
- Fix should be minimal (1-5 lines of code change)
- Error message format must list missing equipment items

# Quality Gates & Verification

Before declaring the task complete, verify all of the following:
1. ✅ `oswe-mini-m23a1s255/fixed_project/` directory exists with complete project structure
2. ✅ Original `issue_project/` remains completely unchanged
3. ✅ All 4+ tests pass (including the 2 previously failing tests)
4. ✅ Smoke test confirms equipment validation correctly rejects partial equipment matches
5. ✅ `README.md` and `FIX_SUMMARY.md` are complete, well-documented, and use relative paths only
6. ✅ No runtime errors, exceptions, or import failures during any test execution
7. ✅ Code follows Python conventions and best practices
8. ✅ Error message format clearly lists missing equipment items (not just "missing equipment")
9. ✅ System behavior is consistent and predictable across all test scenarios
10. ✅ Validation results explicitly state pass/fail status with exact counts

**Success Criteria**: Task is complete when:
- New `oswe-mini-m23a1s255/fixed_project/` directory exists with all files
- `pytest -v` output shows all tests PASSING (e.g., "6 passed in 0.04s")
- Smoke test demonstrates correct equipment validation behavior
- Documentation clearly explains the fix with before/after code examples
- Original project remains unchanged
- **Final response explicitly states: "All test cases passed" with exact test count and status**
- No ambiguity in test results or system status

---

**CRITICAL**: All test results and validation status must be explicitly stated in your final report with exact numbers and clear pass/fail indicators. Do not use vague language like "tests appear to pass" or "likely successful" — provide exact counts and definitive status statements.
